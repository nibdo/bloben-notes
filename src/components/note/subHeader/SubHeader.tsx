import React from 'react';
import './SubHeader.scss';

const SubHeader = (props: any) => {
  const { note, selectedTags, tags } = props;

  return <div className={'subheader__container'}></div>;
};

export default SubHeader;
